Status: completed

## Parent

[PRD: Praxis — Personal AI Agent Framework](../PRD.md)

## What to build

The first tracer bullet: a runnable binary that demonstrates the full pipeline (CLI → Core → Loop → Provider) with protocol types and a hardcoded stub provider. Every module wireframe exists, but the provider just returns a canned greeting.

Creates the module skeleton: `protocol` (shared data types with Serde), `providers` (Provider trait + StubProvider), `loop` (single-turn stub reactor), `core` (thin orchestration), `cli` (arg parsing). `cargo run` prints a greeting.

## Acceptance criteria

- [x] `protocol` crate defines Message, ContentBlock, Role, ToolDef with Serde derive
- [x] `providers` crate defines the Provider trait returning ChatStream (stream of chunks)
- [x] StubProvider returns a single "Hello from Praxis!" chunk
- [x] `loop` crate defines a single-turn run that calls provider and outputs response
- [x] `core` crate wires provider selection and loop invocation
- [x] `cli` parses a minimal arg (no profile yet — just runs)
- [x] Unit tests for protocol serialization round-trips
- [x] `cargo run` prints the greeting and exits successfully

## Blocked by

None — can start immediately.

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Wireframe the full pipeline (CLI → Core → Loop → Provider) with protocol types and a stub provider. `cargo run` prints a greeting.

**Current behavior:**
The codebase is a stub: `src/main.rs` calls `src/lib.rs::run()` which prints "Praxis — personal AI agent framework". No module structure, no types, no provider abstraction.

**Desired behavior:**
The binary demonstrates the complete flow: CLI parses args → Core selects a provider → Loop invokes provider → Provider returns a stream of chunks → Loop assembles and outputs. The provider is a StubProvider that returns "Hello from Praxis!" as a single chunk. All modules exist as crates/modules with their public interfaces, ready for the next slices to fill in real implementations.

**Key interfaces:**
- `Message` — represents a chat message (role + content blocks). Must derive Serialize/Deserialize.
- `ContentBlock` — either a text block or a tool_call/tool_result block. Serde-tagged enum or similar.
- `Role` — enum: System, User, Assistant, Tool.
- `ToolDef` — function definition with name, description, and JSON Schema parameters. Serialized to provider API format.
- `Provider` trait — `async fn chat(&self, messages: Vec<Message>, tools: Vec<ToolDef>) -> Result<ChatStream>`. ChatStream yields chunks.
- `ChatStream` — a stream of chunks (text deltas, tool call deltas, or a finish signal).

**Acceptance criteria:**
- [x] `protocol` module defines Message, ContentBlock, Role, ToolDef with Serde derive
- [x] `providers` module defines the Provider trait returning ChatStream
- [x] StubProvider returns a single "Hello from Praxis!" chunk
- [x] `loop` module defines a single-turn run that calls provider and outputs response
- [x] `core` module wires provider selection and loop invocation
- [x] `cli` parses a minimal arg (no profile yet — just runs)
- [x] Unit tests for protocol serialization round-trips
- [x] `cargo run` prints the greeting and exits successfully

**Out of scope:**
- Profile/configuration loading (that's slice 04)
- Real LLM providers (that's slice 02)
- Tool execution loop (that's slice 03)
- Skill loading (that's slice 05)
- Session persistence (that's slice 06)
