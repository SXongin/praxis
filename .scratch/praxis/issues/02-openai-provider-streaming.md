Status: completed

## Parent

[PRD: Praxis — Personal AI Agent Framework](../PRD.md)

## What to build

Replace the StubProvider with a real OpenAI chat completions adapter implementing the Provider trait. The adapter streams responses via SSE, detects tool calls in the stream, and retries connection failures with exponential backoff. API key comes from the `OPENAI_API_KEY` environment variable.

A user runs `OPENAI_API_KEY=sk-... cargo run` and sees real AI responses streamed to the terminal. At this point, the Loop still runs a single turn (no tool execution loop yet — that comes in the next slice).

## Acceptance criteria

- [x] OpenAI adapter implements Provider trait via `POST /v1/chat/completions`
- [x] Streaming enabled (`stream: true`), chunks parsed from SSE
- [x] Tool call delta accumulation (partial tool calls assembled as chunks arrive)
- [x] Provider returns a ChatStream; Loop assembles final Message from chunks and forwards to CLI
- [x] Connection failure retry: 3 attempts with exponential backoff (inside the adapter, transparent to the loop)
- [x] API key read from `OPENAI_API_KEY` env var; clear error if missing
- [x] Unit tests with a mock HTTP server (wiremock or similar) covering success, streaming, and retry paths
- [x] `cargo run` with a real key produces streamed AI output

## Blocked by

- [01-protocol-types-stub-loop](./01-protocol-types-stub-loop.md)

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Implement a real OpenAI provider adapter with SSE streaming, tool call delta accumulation, and connection retry logic.

**Current behavior:**
Issue 01 provides a StubProvider that returns a hardcoded string. No real LLM integration exists.

**Desired behavior:**
The binary connects to OpenAI's chat completions API, streams responses in real-time, and can detect when the model calls a tool (though tool execution happens in slice 03). API key from `OPENAI_API_KEY` env var. Connection failures are retried internally (3 attempts, exponential backoff) transparent to the loop.

**Key interfaces:**
- `OpenAiProvider` — implements `Provider` trait. Constructed from API key (env var), optional base URL.
- `ChatStream` implementation — an async stream that parses SSE `data:` lines into chunks. Yields `ContentBlock` deltas (text, tool_call name, tool_call arguments, finish).
- Retry logic lives inside the provider adapter, not exposed to the loop.

**Acceptance criteria:**
- [x] OpenAI adapter implements Provider trait via `POST /v1/chat/completions`
- [x] Streaming enabled (`stream: true`), chunks parsed from SSE
- [x] Tool call delta accumulation (partial tool calls assembled as chunks arrive)
- [x] Provider returns a ChatStream; Loop assembles final Message from chunks and forwards to CLI
- [x] Connection failure retry: 3 attempts with exponential backoff (inside the adapter, transparent to the loop)
- [x] API key read from `OPENAI_API_KEY` env var; clear error if missing
- [x] Unit tests with a mock HTTP server covering success, streaming, and retry paths
- [x] `cargo run` with a real key produces streamed AI output

**Out of scope:**
- Anthropic or other provider adapters (V2)
- Tool execution in the loop (slice 03)
- Profile-based provider selection (slice 04)
