Status: completed

## Parent

[PRD: Praxis — Personal AI Agent Framework](../PRD.md)

## What to build

Polish the CLI experience: streaming display with formatted output, tool call indicators, help text, version info, graceful Ctrl+C handling, and friendly error messages for common failure modes. Add integration tests that run the binary end-to-end with mock providers and real tool endpoints.

A user runs `praxis --help` and sees clean usage docs. During a session, they see streaming responses, tool call indicators (e.g., `🔧 Running bash: ls /tmp`), and on Ctrl+C they get a clean exit. If `OPENAI_API_KEY` is missing, they see a clear "set this env var" message rather than a stack trace.

## Acceptance criteria

- [x] Streaming output displayed as chunks arrive (not buffered)
- [x] Tool call execution shown with tool name and arguments
- [x] `--help` and `--version` flags work and explain all options
- [x] Ctrl+C triggers graceful shutdown (saves session if applicable, clean exit)
- [x] Friendly error messages for: missing API key, network down, invalid profile, profile not found, missing skills directory
- [x] Integration tests: binary invoked with mock HTTP server, verify stdout/stderr behavior
- [x] Integration tests: binary with real temp filesystem for tool tests
- [x] All modules have test coverage on public interfaces (as defined in PRD testing decisions)

## Blocked by

- [07-context-window-loop-guard](./07-context-window-loop-guard.md)

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Polish the CLI output experience (streaming display, tool indicators, help text, Ctrl+C) and add integration tests covering the full binary.

**Current behavior:**
The CLI has minimal output — raw text chunks may be printed without formatting. Help text is basic. No integration tests run the binary end-to-end.

**Desired behavior:**
The CLI feels polished: streaming text appears as chunks arrive, tool calls are announced with name and arguments, `--help` and `--version` are complete, Ctrl+C triggers graceful shutdown, and common error cases show friendly messages instead of stack traces. Integration tests exercise the binary with mock providers and real tool endpoints.

**Key interfaces:**
- CLI display formatting: as ChatStream chunks arrive, print text deltas immediately (no buffering). Tool call deltas should show indicator like `🔧 bash: ls /tmp`. Tool results should show output or error summary.
- Signal handler: on SIGINT (Ctrl+C), trigger session save (if applicable), print "Goodbye", exit 0.
- Error display: map known error types (ProviderError::NoApiKey, ProfileError::NotFound, etc.) to user-friendly messages. Unknown errors get a generic message with suggestion to run with `--verbose` for details.
- Clap or similar for argument parsing: `--profile`, `--session`, `--save`, `--list-sessions`, `--help`, `--version`.
- Integration tests: spawn binary with a mock HTTP server, assert stdout contains expected streaming output, assert stderr behavior on errors.

**Acceptance criteria:**
- [x] Streaming output displayed as chunks arrive (not buffered)
- [x] Tool call execution shown with tool name and arguments
- [x] `--help` and `--version` flags work and explain all options
- [x] Ctrl+C triggers graceful shutdown (saves session if applicable, clean exit)
- [x] Friendly error messages for: missing API key, network down, invalid profile, profile not found, missing skills directory
- [x] Integration tests: binary with mock HTTP server, verify stdout/stderr
- [x] Integration tests: binary with real temp filesystem for tool tests
- [x] All modules have test coverage on public interfaces

**Out of scope:**
- TUI/GUI frontend
- Progress bars or rich formatting
- Colored terminal output (plain text is fine for V1)
- Benchmarking or performance tests
